/**
 * Test file in subdirectory X
 */

describe('Sample test in X directory', () => {
  /**
   * @given a test file in subdirectory X
   * @when running the documentation generator
   * @then it should preserve the folder structure in output
   */
  it('should preserve folder structure', () => {
    expect(true).toBe(true);
  });
});
